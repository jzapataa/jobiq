spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/jobiq}
    username: ${DB_USERNAME:jobiq}
    password: ${DB_PASSWORD:jobiq-local}
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.jobiq: DEBUG
