spring:
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

jobiq:
  security:
    jwt:
      secret: jobiq-test-jwt-secret-minimum-32-characters-2026
      issuer: jobiq-api-test
      ttl: PT8H

management:
  endpoint:
    health:
      show-details: never
      show-components: never
